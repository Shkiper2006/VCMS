# Default theme

Starter theme directory. Add templates, layouts, assets, and theme-specific configuration here.

`theme.json` declares the theme name, version and minimal template map for home, post, page, category and search views. The active theme is managed by `ThemeRegistry` in `src/main.ts`.
