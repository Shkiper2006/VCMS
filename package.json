{
  "name": "vcms",
  "version": "0.1.0",
  "private": true,
  "description": "VCMS — minimal custom CMS engine core.",
  "scripts": {
    "dev": "echo \"Start backend (NestJS) and admin (React) after installing dependencies\"",
    "dev:backend": "echo \"Run NestJS API from src/ after dependencies are installed\"",
    "dev:admin": "npm --workspace @vcms/admin run dev",
    "migrate": "echo \"Run MySQL migrations from database/migrations/\"",
    "check": "tsc --noEmit",
    "build:admin": "npm --workspace @vcms/admin run build",
    "check:admin": "npm --workspace @vcms/admin run check"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "workspaces": [
    "frontend/admin"
  ]
}
