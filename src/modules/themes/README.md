# Themes module

Минимальная реализация находится в `ThemeRegistry` в `src/main.ts`.

Реестр хранит активную тему и ее manifest. Тема по умолчанию описана в `themes/default/theme.json`.

План развития: загрузка шаблонов из темы, asset pipeline, preview темы и настройка layout-областей.
