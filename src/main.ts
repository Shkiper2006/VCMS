/**
 * Backend entry point placeholder for the VCMS engine.
 *
 * Planned stack: Node.js + NestJS API, PostgreSQL persistence, and a React admin UI.
 */
export function bootstrap(): string {
  return 'VCMS backend bootstrap placeholder';
}
